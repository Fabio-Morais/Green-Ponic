#include <stdlib.h>
#include <string.h>
#include "vetor.h"
#include "Message.h"

int main()
{

   vetor *vec=vetor_novo();
    vetor *vec=vetor_novo();
    Room *room=NULL;
    leFicheiroConfiguration(vec);
    leFicheiroRules(vec,room);
    return 0;
}
